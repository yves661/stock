# Mini Shop Manager — Demo

Simple static demo replicating the attached landing hero using HTML, CSS, JavaScript and Bootstrap icons.

How to run

Open `index.html` in your browser (double-click or serve from a local static server).

Quick local server (Python 3):

```bash
python -m http.server 8000
# then open http://localhost:8000 in your browser
```
